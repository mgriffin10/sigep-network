<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
     <%= link_to( image_tag('gu-sigep.png', :width => 180, :height => 50, :alt => 'GU | SigEp'), 
                  {:action => 'index', :controller => 'main'}, 
                  {:class => 'navbar-brand-custom'}) %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      
      <ul class="nav navbar-nav">
      <li><%= link_to("Network", :controller => "profiles") %></li>
      <% if !student? %>
        <% if profile_exists? %>
           <li><%= link_to("Profile", :controller=> "profiles", :action => "show", :id => session[:user_id], :user_id => true) %><span class="sr-only">(current)</span></li>
        <% else %>
          <li><%= link_to("Complete Profile", :controller => "profiles", :action => "new", :id => session[:user_id]) %><span class="sr-only">(current)</span></li>
        <% end %>
      <% end %>
      </ul>

      <ul class="nav navbar-nav navbar-right">
      <% if !student? %>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">Account <span class="caret"></span></a>
          <ul class="dropdown-menu" role="menu">
            <% if profile_exists? %>
            <li><%= link_to("Edit Profile", :controller=> "profiles", :action => "edit", :id => session[:user_id], :user_id => true) %></li><% end %>
            <li><%= link_to("Edit Login Email", :controller => "users", :action => "edit") %></li>
          </ul>
        </li>
        <% end %>
        <li><%= link_to("Sign out", :controller => "access", :action => "logout") %></li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>